<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.digitalindividual.billayer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".app.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".app.EscolherCadastro" />
        <activity android:name=".app.Cadastro" />
        <activity android:name=".app.CadastroCategoria" />
        <activity android:name=".app.LancamentosActivity" />
        <activity android:name=".app.VisualizarActivity" />
        <activity android:name=".app.CategoriasActivity"></activity>
    </application>

</manifest>